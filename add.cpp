
#include <iostream>

using namespace std;

#define ADD
//we add this so that we can check If we have already written this code or not.


int add(int x, int y)
{
	return (x + y);
}

//This is first files which will be on top.   next is subtract.cpp

